name: Hello world
on: workflow_dispatch
jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - name: Build job steps
     run: echo " A devops build"
 test:
    runs-on: ubuntu-latest
    steps:
     - name: test phase
     run: echo "running test cases"

     deploy: 
      runs-on: ubuntu-latest
      steps:
      - name: deploy the app
        run: echo "Deploying the app"
    
   
 
